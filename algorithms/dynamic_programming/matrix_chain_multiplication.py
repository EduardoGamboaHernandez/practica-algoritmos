def matrix_chain_order(dims: list[int]) -> int:
    """
    Encuentra el número mínimo de multiplicaciones escalares necesarias
    para multiplicar una cadena de matrices.
    :param dims: Una lista de dimensiones. dims[i-1]xdims[i] es la dimensión de la matriz i.
    :return: El número mínimo de multiplicaciones.
    """
    # Escribe tu lógica de programación aquí
    pass
