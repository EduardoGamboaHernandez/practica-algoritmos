def prim(graph: dict, start_node):
    """
    Encuentra el Árbol de Expansión Mínima (Minimum Spanning Tree) para un grafo
    conexo y ponderado utilizando el algoritmo de Prim.
    :param graph: Un diccionario que representa el grafo (igual que en Dijkstra).
                  Ej: {'A': {'B': 2, 'C': 3}, 'B': {'A': 2, 'C': 1, 'D': 1}}
    :param start_node: El nodo desde el cual comenzar a construir el MST.
    :return: Una lista de tuplas, donde cada tupla representa una arista (u, v, peso)
             en el MST.
    """
    # Escribe tu lógica de programación aquí
    pass
