def dijkstra(graph: dict, start_node):
    """
    Encuentra el camino más corto desde un nodo de inicio a todos los demás nodos
    en un grafo ponderado utilizando el algoritmo de Dijkstra.
    :param graph: Un diccionario que representa el grafo. Las claves son los nodos
                  y los valores son diccionarios de vecinos con sus pesos.
                  Ej: {'A': {'B': 1, 'C': 4}, 'B': {'A': 1, 'C': 2, 'D': 5}}
    :param start_node: El nodo desde el cual comenzar.
    :return: Un diccionario con las distancias más cortas desde el nodo de inicio
             a cada uno de los otros nodos.
    """
    # Escribe tu lógica de programación aquí
    pass
