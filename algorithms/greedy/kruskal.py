def kruskal(graph_edges: list[tuple]):
    """
    Encuentra el Árbol de Expansión Mínima (Minimum Spanning Tree) para un grafo
    conexo y ponderado utilizando el algoritmo de Kruskal.
    :param graph_edges: Una lista de tuplas, donde cada tupla representa una
                        arista con (nodo1, nodo2, peso).
                        Ej: [('A', 'B', 7), ('A', 'D', 5), ('B', 'C', 8)]
    :return: Una lista de tuplas que representan las aristas del MST.
    """
    # Escribe tu lógica de programación aquí
    pass
